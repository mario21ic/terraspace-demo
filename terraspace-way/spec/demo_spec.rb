describe "demo" do
  it "the truth" do
    expect(true).to be true
  end

  it "default_random_pet_length" do
    #expect(default_random_pet_length).to eq 2
    expect(default_random_pet_length).to eq 3
  end
end
